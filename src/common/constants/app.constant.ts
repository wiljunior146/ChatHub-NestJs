/**
 * The string key name for roles.
 * 
 * @type {String}
 */
export const ROLES_KEY = 'roles';

/**
 * The salt to be used to hash the password.
 * 
 * If specified as a number then a salt will be generated
 * with the specified number of rounds and used.
 *
 * @type {Number|String}
 */
export const SALT_OR_ROUNDS = 10;
