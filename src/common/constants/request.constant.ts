/**
 * The property name to be injected on the dto to be used on custom validator.
 * 
 * @note This property name will be used to all custom injected not just the
 *       current user Id so we can easily identify the injected property.
 * @type {String}
 */
export const REQUEST_CONTEXT = '_requestContext';
